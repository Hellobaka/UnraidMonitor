<UserControl
    x:Class="me.cqp.luohuaming.UnraidMonitor.UI.Views.Controls.CommandListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:me.cqp.luohuaming.UnraidMonitor.UI.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="850"
    d:DesignWidth="500"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <hc:SearchBar
            x:Name="SearchBar"
            Margin="5,5,5,0"
            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
        <ListBox
            x:Name="CommandList"
            Grid.Row="1"
            Margin="5"
            DisplayMemberPath="Command"
            ItemsSource="{Binding CommandList}"
            SelectedItem="{Binding SelectedCommand, Mode=TwoWay}"
            SelectionMode="Single">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Command="{Binding CopyCommand}" Header="复制" />
                    <MenuItem Command="{Binding MoveUpCommand}" Header="上移" />
                    <MenuItem Command="{Binding MoveDownCommand}" Header="下移" />
                    <MenuItem Command="{Binding RemoveCommand}" Header="删除" />
                </ContextMenu>
            </ListBox.ContextMenu>
        </ListBox>
        <Grid Grid.Row="2" Margin="5,0,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="0"
                HorizontalAlignment="Stretch"
                Command="{Binding AddCommand}"
                Content="添加" />
            <Button
                Grid.Column="1"
                Margin="5,0,0,0"
                HorizontalAlignment="Stretch"
                Command="{Binding ImportCommand}"
                Content="导入" />
            <Button
                Grid.Column="2"
                Margin="5,0,0,0"
                HorizontalAlignment="Stretch"
                Command="{Binding ExportCommand}"
                Content="导出" />
            <Button
                Grid.Column="3"
                Margin="5,0,0,0"
                HorizontalAlignment="Stretch"
                Command="{Binding SaveAllCommand}"
                Content="保存全部" />
        </Grid>
    </Grid>
</UserControl>